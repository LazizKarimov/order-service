<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="002-create-outbox-events" author="laziz">
        <createTable tableName="outbox_events">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="aggregate_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="event_type" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="topic" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="payload" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="processed_at" type="TIMESTAMP"/>
        </createTable>

        <!--
          Индекс для publisher'а: он ищет записи WHERE processed_at IS NULL ORDER BY created_at.
          Для маленьких объёмов не критичен, но заложим сразу.
        -->
        <createIndex tableName="outbox_events" indexName="ix_outbox_unprocessed">
            <column name="processed_at"/>
            <column name="created_at"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>